server:
  port: 8000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java_blog?serverTimezone=UTC-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 表内有数据时不会清空, 只会更新
    # 控制台显示SQL
    show-sql: true